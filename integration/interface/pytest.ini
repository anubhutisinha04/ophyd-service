[pytest]
# Standalone config for the cross-service interface suite. Kept separate from
# the per-backend pyproject.toml so it doesn't inherit their coverage gates
# (this suite spans two packages, so single-package coverage is meaningless).
asyncio_mode = auto
testpaths = .
